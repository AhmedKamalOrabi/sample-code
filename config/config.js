/*
 **  Put here all configuration variables that your plugin needed
 **  and that depend on changing environment [Ex: from staging to production]
 **  some examples: baseApiUrl, Navigation links from plugin to others...etc
 */

export default {
  baseUrl: 'http://localhost:3000',
};
